# 실행 방법

`reverse_runner.py`, `reverse_qa.py`를 `rag_qna_multi.py`와 같은 폴더에, `reverse_prompt.py`는 `prompts/` 폴더 안에 넣는다.

```bash
python reverse_runner.py
```

실행 후 1(소화제 시나리오), 2(진통제 시나리오), 3(직접 입력) 중 선택한다.
